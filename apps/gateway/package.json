{
  "name": "@atomic/gateway",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/cli.ts",
    "start": "tsx src/cli.ts",
    "test": "vitest run",
    "test:contract": "vitest run src/contract.test.ts",
    "test:integration": "vitest run src/integration.test.ts"
  },
  "dependencies": {
    "@atomic/gateway-core": "workspace:*",
    "@atomic/gateway-providers": "workspace:*",
    "@fastify/static": "^8.1.0",
    "fastify": "catalog:",
    "js-tiktoken": "^1.0.19",
    "prom-client": "^15.1.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
