{
  "name": "@atomic/orchestrator",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:integration": "vitest run src/server.integration.test.ts",
    "test:vault": "vitest run --project vault"
  },
  "dependencies": {
    "@atomic/db": "workspace:*",
    "@atomic/mcp-hub": "workspace:*",
    "@atomic/shared": "workspace:*",
    "@atomic/vault": "workspace:*",
    "@fastify/websocket": "catalog:",
    "drizzle-orm": "catalog:",
    "fastify": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
